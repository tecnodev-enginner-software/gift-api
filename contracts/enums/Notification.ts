enum Notification {
  // generic notification
  DEFAULT = 'default',
  // user account notification
  ACCOUNT = 'account',
  // user profile notification
  PROFILE = 'profile',
  // budget notification
  BUDGET = 'budget',
}

export default Notification
